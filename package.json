{
  "name": "ticketing-platform-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "devDependencies": {
    "turbo": "^2.1.1"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test"
  }
}

